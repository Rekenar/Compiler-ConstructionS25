package task1;

public class HexDez {

    /**
     *
     * private static int H() {
     *     int s;
     *     expect("0x");          // match "0x"
     *     s = N();
     *     return s;,
     * }



     * private static int N() {
     *     int b = 0;
     *     b = D();
     *     while (nextTokenIsDigitOrLetter()) {
     *         int v = D();
     *         b = b + v;
     *     }
     *     return b;
     * }


     * private static int D() {
     *     int v;
     *     if (currentTokenIsDigit()) {
     *         v = digitValue(); // assume digitValue() gives 0..9
     *         scan(); // move to next token
     *     } else if (currentTokenIsLetter()) {
     *         v = castInt(letterValue()); // assume letterValue() is 'A', 'B', etc.
     *         scan();
     *     } else {
     *         error("Expected digit or letter");
     *         v = 0; // fallback
     *     }
     *     return v;
     * }
     */

}
